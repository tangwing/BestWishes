# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Secrets — never commit
.env
*.env
.env.*

# Populated once the tech stack is chosen (node_modules/, build/, Pods/, DerivedData/, etc.)
node_modules/
dist/
build/
*.tsbuildinfo

# 本地 PGlite 落盘目录
.pgdata/
*.pgdata/

# 本地音频文件落盘目录
.audio-data/
*.audio-data/

# demo 自动重启 hook 的状态目录（签名标记 + 日志，机器本地，不进版本库）
.claude/.demo-state/
