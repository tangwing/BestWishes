{
  "name": "bestwishes-e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "端到端测试（Playwright）。独立于 monorepo：自己的 npm 依赖，不进 pnpm workspace。",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2"
  }
}
