> 实现前先读 [design.md](design.md) 的 D1–D6，**D1（图片生成方案）必须先定再写代码**。基线假定 `redesign-kindness-entry` 已 apply（三档权限沿用它，不重新定义）。

## 1. 卡片生成的地基

- [ ] 1.1 定下 D1 的光栅化库与中文字体资产，在 design.md 里把"倾向"改成"已定"并写明版本；`pnpm verify` 绿，说明依赖装得上、跑得起来
- [ ] 1.2 中文断行纯函数（CJK 逐字可断、连续拉丁不拆、标点避头尾、超长截断），单测覆盖：纯中文 / 纯英文长单词 / 中英混排 / 连续标点 / 超长截断（见 design D2）
- [ ] 1.3 SVG 版面模板（纯字符串，颜色字号对齐 `theme.css` 的 token），产出一份可肉眼检查的 SVG，确认版面与产品视觉同源
- [ ] 1.4 光栅化成 PNG + 嵌入字体；验证：生成一张含中文的卡片，图里是中文而不是豆腐块

## 2. 卡片的服务端能力

- [ ] 2.1 卡片缓存挂在既有存储 port 后面（与音频同一抽象，不新建存储概念）；key = 祝福 id + 内容哈希（见 design D6）
- [ ] 2.2 确定性验证：同一条祝福连续生成两次，产物字节一致（这是缓存能成立的前提）
- [ ] 2.3 状态门禁——请求卡片时先查祝福当前状态，非 `published` 直接返回中性占位、**根本不碰缓存**；测试：撤回后请求卡片不含原正文
- [ ] 2.4 隐私断言测试：生成的卡片数据里不含 openid、精确经纬度、手机号、收件人身份
- [ ] 2.5 超长正文：卡片优雅截断 + 省略标识，版面不溢出（对应 spec 场景）

## 3. 落地页升级为传播载体

- [ ] 3.1 `/p/:slug` 的 `published` 响应输出 `og:title` / `og:description` / `og:image`（指向卡片）+ `twitter:card`，且卡片图在 HTML 中靠前出现（见 design D3 第一层）
- [ ] 3.2 非 `published` 状态输出不含原内容的通用分享图与中性描述；测试断言撤回后的页面 meta 里没有原正文
- [ ] 3.3 **音频祝福的播放器**（B-93 明确遗留的缺口）：落地页给出音频入口；未登录显示"登录后可收听"，登录后可播放——沿用 `redesign-kindness-entry` 定下的三档权限，不新定义
- [ ] 3.4 e2e：分享一条祝福链接，抓取页面 meta，确认 `og:image` 可访问且返回图片

## 4. 文字祝福的用心反馈

- [ ] 4.1 复用现有真诚度 / 个性化评估器与 `isLowEffort` / `looksGarbled`，组装出**文字版**的多维反馈（见 design D4）；**不新建评分体系**
- [ ] 4.2 单测断言反馈里**不含**专注度 / 流畅度（依赖音频信号的维度）——这条断言防的是后人"顺手对齐音频反馈"把假维度塞回来
- [ ] 4.3 单测断言输出里没有任何单一总分 / 星级字段（与 `audio-scoring` 同一契约）
- [ ] 4.4 反馈只对作者可见：收件人视图里不含任何反馈字段，测试断言之
- [ ] 4.5 反馈失败 / 超时不阻断发送（见 design D5）：注入一个必然失败的评估器，断言祝福仍然 `published` 且已投递，结果页显示"反馈暂不可用"

## 5. 前端：发送后的那 5 秒

- [ ] 5.1 结果页改版：卡片 + 用心反馈 + 保存图片 + 分享入口，一屏之内
- [ ] 5.2 被拒时**不出现**卡片与分享入口，只有拒绝原因与大类——B-76 修过的"已发送 ✔ 和 未通过审核 同时出现"的矛盾不得回潮，补一条回归测试钉住
- [ ] 5.3 `Sent.tsx` 与 `AudioFeedback.tsx` 的成功态统一到同一个产物组件（反馈展示结构同构，见 design D4）
- [ ] 5.4 福袋里给收件人一个分享入口（分享的是该祝福既有的公开落地页，可见性不扩大）
- [ ] 5.5 发送页加一句说明"这条祝福有公开链接，对方可以分享它"（design 风险项：分享让公开这件事有了新的心理后果，要提前说清）

## 6. 验收

- [ ] 6.1 e2e：发一条文字祝福 → 结果页同时出现卡片与多维反馈 → 保存图片可用
- [ ] 6.2 e2e：被审核拒绝的祝福 → 结果页没有卡片、没有分享入口、没有"已发送"字样
- [ ] 6.3 视觉走查：最长正文、最短正文、纯英文、中英混排四种卡片各看一眼，确认不贴边不溢出
- [ ] 6.4 `pnpm verify` + `pnpm test:e2e` + `openspec validate --strict` 全绿
- [ ] 6.5 `docs/DEMO.md` 补分享走查；BACKLOG 恢复点更新
- [ ] 6.6 复查「已分发副本不可追回的告知」的现有文案是否覆盖了"分享预览图可能滞留在第三方平台"这一情形（design 风险项）；不覆盖则记进 BACKLOG，**不在本变更里顺手改那条 Requirement**

## 7. 阻塞项（拿到资质前不做，占位记录）

- [ ] 7.1 微信 JS-SDK 自定义分享——**阻塞于**公众号 appId + 已备案域名（B-42）。在此之前 og 元信息那层已能在微信里得到预览；拿到资质后再开一个独立 change，不在本变更内实现
